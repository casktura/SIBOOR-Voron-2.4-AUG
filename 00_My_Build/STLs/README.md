# Parts Precedence

1. Any files from `00_Prints`, files inside subfolder take place over the other folder (in current folder) with the same name.
2. Files from other places in this folder.
3. Default files for Voron 2.4 R2 which found in `STLs` folder in project root.
